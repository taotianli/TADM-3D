# torch and torchvision are pre-installed in the NVIDIA container
# torch>=2.1.0
# torchvision>=0.16.0

# Medical imaging
monai==1.5.2
# monai-generative installed separately with --no-deps to avoid pulling torch
nibabel==5.3.2
simpleitk==2.5.2

# Data / numerics
# numpy<2 required for compatibility with container's pyarrow and cv2
numpy==1.26.4
pandas==2.2.3
scipy==1.13.1
scikit-image==0.24.0

# Model components
einops==0.8.1
timm==1.0.22

# Training utilities
tqdm==4.67.1
tensorboard==2.20.0
wandb==0.22.2
